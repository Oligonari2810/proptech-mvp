global:
  # Console-only for now (no SMTP/Slack configured)

route:
  group_by: ['alertname', 'environment']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'console-only'

receivers:
  - name: 'console-only'
    # View alerts in Alertmanager UI


